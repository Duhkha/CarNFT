export type Car = {
    tokenId: number;
    price: number; 
    name: string;
    img: string;
    externalUrl: string; 
  };

  export type NFTMetadata = {
    img: string;
    externalUrl: string;
  };
  